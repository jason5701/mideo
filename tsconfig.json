{
  "compilerOptions": {
    // ▼ 이 부분들을 바꿔주시옵소서
    "target": "esnext",
    "module": "esnext", // <--- 여기가 핵심 해결책이옵니다!
    "moduleResolution": "node", // 또는 "bundler" (버전에 따라 다름)
    "lib": ["dom", "dom.iterable", "esnext"],

    // ▼ Vite의 환경변수(import.meta.env)를 인식하기 위해 필수
    "types": ["vite/client"],

    "allowJs": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "noFallthroughCasesInSwitch": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx"
  },
  "include": ["src"]
}
